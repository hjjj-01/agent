# ============================================================
# LangChain 1.x 核心库
# ============================================================
# LangChain 1.0 是一次重大升级，核心变化：
#   - create_agent() 新 API 替代了 create_tool_calling_agent + AgentExecutor
#   - 基于 LangGraph 构建，内置工具调用循环（无需手动管理）
#   - 消息列表格式统一（输入输出都用 {"messages": [...]}）
# ============================================================
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
# langchain-community 目前最新是 0.4.x，还没有 1.0 版本
# 提供 SentenceTransformer/DashScope embeddings、文档加载器等
langchain-community>=0.4.0

# LangGraph（create_agent 的底层引擎，必须安装）
# LangGraph 是一个有状态的多角色应用程序框架
# create_agent 内部使用 langgraph.prebuilt.create_react_agent
langgraph>=0.6.0

# ============================================================
# OpenAI API（用于LLM）
# ============================================================
openai>=2.0.0

# ============================================================
# 向量数据库（用于RAG知识库）
# ============================================================
# ChromaDB：轻量级向量数据库，支持本地持久化
chromadb>=0.5.0

# LangChain 专用 Chroma 适配器（1.x 推荐方式）
# langchain-community 里的 Chroma 仍然可用，但 langchain_chroma 是新官方路径
langchain-chroma>=0.2.0

# 文本分割器（RecursiveCharacterTextSplitter 等）
# 1.x 中从独立的 langchain_text_splitters 包导入
langchain-text-splitters>=0.3.0

# ============================================================
# Embedding 模型支持
# ============================================================
# 本地 SentenceTransformer 模型（用于离线或本地 embedding）
# sentence-transformers>=3.0.0

# ============================================================
# 飞书SDK（消息机器人）
# ============================================================
lark-oapi>=1.7.0

# ============================================================
# 阿里云DashScope（用于阿里云 embedding 模型）
# ============================================================
dashscope>=1.17.0

# ============================================================
# Web框架（用于飞书机器人接收消息）
# ============================================================
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.3

# ============================================================
# HTTP请求（用于WMS接口调用）
# ============================================================
requests>=2.31.0
httpx>=0.26.0

# ============================================================
# 环境变量管理
# ============================================================
python-dotenv>=1.0.0

# ============================================================
# 数据处理
# ============================================================
pandas>=2.1.4
numpy>=1.26.2

# ============================================================
# 日志
# ============================================================
loguru>=0.7.2

# ============================================================
# 测试
# ============================================================
pytest>=7.4.4
pytest-asyncio>=0.23.3

# ============================================================
# 异步支持
# ============================================================
aiohttp>=3.9.1
